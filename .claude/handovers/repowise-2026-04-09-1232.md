# Handover: repowise - 2026-04-09 12:32

## Current Task
Fixing `repowise init` performance issues with Ollama local inference — dead code detection, decision extraction, and test-run scoping.

## Status
- [x] Dead code detection hang — fixed (asyncio.to_thread)
- [x] Decision extraction file scan hang — fixed (asyncio.to_thread + _SKIP_DIRS for Elixir)
- [x] --test-run not limiting files — fixed (removed generate_docs gate)
- [x] --test-run generating too many pages — fixed (file_page only filter)
- [x] --test-run still running Phase 2 — fixed (skip analysis entirely)
- [x] Progress bars never closing — fixed (on_phase_complete protocol)
- [x] Decision extraction LLM calls too slow on ollama — fixed (skip LLM sources for ollama)
- [ ] Decision extraction still takes ~34s on Elixir repo — _SKIP_DIRS fix just applied, UNTESTED
- [ ] Embedder defaults to mock with ollama — no ollama embedder exists yet
- Current branch: `feat/elixir-language-support`
- Last commit: `a86a923 - chore: session handover - tool comparison and strategy finalisation`
- All changes uncommitted (5 files modified)

## Immediate Next Steps
1. **Test the _SKIP_DIRS fix** — run `rwi -y` on find-my-royalties, decision extraction should now be fast (was scanning 6k files in deps/_build)
2. **Test the ollama provider_name check** — verify decision extraction skips LLM calls for ollama (the `getattr(decision_llm, "provider_name", "") == "ollama"` check may not be firing)
3. **Let infranav-v2 full run complete** — Angular/TS repo was running 1/281 pages when session ended
4. **Verify all fixes together** — full `rwi -y` on both repos should work end-to-end
5. **Clean up guide.md** before PR

## Blockers / Open Questions
- **Embedder for ollama**: repowise only supports gemini/openai/mock embedders. Ollama has `nomic-embed-text` but no embedder provider exists. Could use OpenAI embedder pointed at Ollama endpoint (like ChunkHound does) but needs model override. Park for separate PR.
- **Decision extraction ollama skip may not be firing**: The `provider_name` property returns `"ollama"` correctly in isolation, but the 34s runtime suggests LLM calls may still be happening. Could be the file scan itself (now fixed with _SKIP_DIRS) or the LLM skip not working. Needs testing.

## Key Decisions Made
- **--test-run skips Phase 2 entirely**: Analysis (dead code + decisions) is irrelevant for a provider validation run
- **--test-run limits generation to file_page type only**: No symbol spotlights, module pages, overview — just the 10 file pages
- **asyncio.to_thread for sync-in-async**: Dead code `analyze()` and decision extractor `_scan_files_for_markers()` wrapped in thread pool
- **Skip LLM decision sources for ollama**: Only inline marker scan (regex) runs; git archaeology and readme mining need fast cloud APIs
- **on_phase_complete protocol**: Progress bars now hide when phase finishes — added to ProgressCallback protocol, LoggingProgressCallback, and RichProgressCallback
- **_SKIP_DIRS expanded**: Added deps, _build, tmp, .elixir_ls, vendor, target for Elixir/general language support

## Context the Next Session Needs
- **Modified files** (all uncommitted):
  - `packages/core/src/repowise/core/pipeline/orchestrator.py` — test-run filter, Phase 2 skip, asyncio.to_thread, on_phase_complete calls, ollama LLM skip
  - `packages/core/src/repowise/core/analysis/decision_extractor.py` — _scan_files_for_markers extracted, _SKIP_DIRS expanded
  - `packages/core/src/repowise/core/pipeline/progress.py` — on_phase_complete added to protocol
  - `packages/cli/src/repowise/cli/ui.py` — on_phase_complete implementation (hides bar)
  - `packages/cli/src/repowise/cli/commands/init_cmd.py` — test-run file_page filter for generation plan
- **Alias config**: `~/.oh-my-zsh/custom/zsh_alias.sh` has `rwi` alias + REPOWISE_PROVIDER/OLLAMA_BASE_URL exports
- **Editable install**: `uv tool install -e` was run, binary at `/home/johan/.local/bin/repowise` points to working tree
- **guide.md**: Created in project root as temporary reference, to be cleaned up before PR
- **Test repos**: find-my-royalties (Elixir, 549 files), infranav-v2 (Angular/TS, 984 files)

## Running Processes
- infranav-v2 may still be running a full `rwi -y` (was at page 1/281 when session ended)
